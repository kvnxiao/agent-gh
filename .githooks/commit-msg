#!/bin/sh
# Claude Code sets CLAUDECODE and Codex sets CODEX_THREAD_ID in the shells that run agent commands.
if [ -n "${CLAUDECODE:-}" ] || [ -n "${CODEX_THREAD_ID:-}" ]; then
  git interpret-trailers --in-place --if-exists addIfDifferent \
    --trailer "Co-authored-by: kvnxiao-agent[bot] <332833177+kvnxiao-agent[bot]@users.noreply.github.com>" \
    "$1"
fi
